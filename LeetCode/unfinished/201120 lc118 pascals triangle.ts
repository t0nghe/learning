// 201120 lc118. Pascal's Triangle

function generate(numRows: number) {

    let chart: Array<Array<number>> = [];
    chart.push([1]);
    chart.push([1,1]);
    

    console.log(chart)
};

generate(1)